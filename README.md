# individual-task3-variant-3

# Ассемблер и интерпретатор для Учебной Виртуальной Машины (УВМ)

## Вариант 3

### Описание проекта
Проект реализует ассемблер и интерпретатор для учебной виртуальной машины согласно спецификации УВМ.

### Реализованные этапы:

#### 🎯 Этап 1: Перевод программы в промежуточное представление
**Цель:** Создать CLI-приложение ассемблера с разбором текстового представления команд.

**Выполнено:**
- ✅ Создано CLI приложение с аргументами командной строки
- ✅ Разработан человекочитаемый язык ассемблера в формате YAML
- ✅ Поддержаны все 4 команды спецификации УВМ
- ✅ Реализован транслятор YAML → промежуточное представление
- ✅ Режим тестирования с выводом промежуточного представления
- ✅ Тестовая программа из спецификации УВМ

#### 📋 Спецификация УВМ (Вариант 3)

*Все команды имеют размер 4 байта*

1. **Загрузка константы** (код 29)
   - Формат: `A=29, B=константа, C=адрес`
   - Операция: `R[C] = B`
   - Тест: `A=29, B=515, C=4` → `0xDD, 0x80, 0x00, 0x10`

2. **Чтение из памяти** (код 18)
   - Формат: `A=18, B=адрес_результата, C=адрес_памяти`
   - Операция: `R[B] = M[R[C]]`
   - Тест: `A=18, B=0, C=2` → `0x12, 0x10, 0x00, 0x00`

3. **Запись в память** (код 9)
   - Формат: `A=9, B=адрес_памяти, C=адрес_источника`
   - Операция: `M[R[B]] = R[C]`
   - Тест: `A=9, B=13, C=24` → `0x49, 0xC3, 0x00, 0x00`

4. **Абсолютное значение** (код 25)
   - Формат: `A=25, B=адрес_памяти, C=адрес_источника`
   - Операция: `M[R[B]] = abs(R[C])`
   - Тест: `A=25, B=26, C=22` → `0x99, 0xB6, 0x00, 0x00`

### Запуск
```bash
# Установка зависимостей
pip install pyyaml

# Запуск в режиме тестирования
python main.py test_program.yaml output.bin --test